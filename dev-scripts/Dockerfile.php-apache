FROM xfrocks/xenforo:php-apache-7.1.6

RUN pear install PHP_CodeSniffer \
    && a2enmod rewrite

COPY docker/* /usr/local/bin/
RUN chmod +x /usr/local/bin/cmd.sh \
    && chmod +x /usr/local/bin/find-addons.sh

CMD ["/usr/local/bin/cmd.sh"]
