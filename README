XenForo Developer Helper add-on

[INSTALLATION]
Copy files and import the xml file as usual.
If you want to help developing this add-on. It's recommended to clone the repo elsewhere then create symbolic links for js/DevHelper and library/DevHelper library.

[FEATURES]
1. Data management with AdminCP interface built-in. Automatically generates DataWriter, Model, Route PrefixAdmin, ControllerAdmin.
2. Install/Uninstall script (built on top of the data management classes)
3. File export (with file sums added, you still need to write the code event listener though)
4. Template editor in AdminCP (only support public templates, powered by ACE)